"""
Auto-generated products resource.

DO NOT EDIT THIS FILE MANUALLY.
Run `python scripts/generate_resources.py` to regenerate.
"""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

if TYPE_CHECKING:
    from ..client.base import BaseClient


class ProductsResource:
    """
    products operations.
    """

    def __init__(self, client: BaseClient) -> None:
        self._client = client

    def _build_params(self, **kwargs: Any) -> dict[str, Any]:
        """Build query parameters, filtering None and empty-string values.

        Empty strings are filtered because MCP tool wrappers pass ``""`` as the
        default for optional string args, and the API rejects empty query
        values (e.g. ``platform=``) with a 400. Filtering here keeps both direct
        SDK callers and MCP tool callers safe.

        Enum members are unwrapped to their value: httpx serializes params
        via str(), which yields "ClassName.MEMBER" for Enum members, so
        e.g. ``status=PostStatus.FAILED`` would otherwise reach the API as
        ``status=PostStatus.FAILED`` instead of ``status=failed``.
        """
        from enum import Enum

        def to_camel(s: str) -> str:
            parts = s.split("_")
            return parts[0] + "".join(p.title() for p in parts[1:])

        result: dict[str, Any] = {}
        for k, v in kwargs.items():
            if isinstance(v, Enum):
                v = v.value
            if v is None or v == "":
                continue
            result[to_camel(k)] = v
        return result

    def _build_payload(self, **kwargs: Any) -> dict[str, Any]:
        """Build request payload, filtering None values. Enum members are
        unwrapped to their value so JSON bodies carry e.g. "failed" rather
        than a raw Enum member (plain Enum members are not JSON-serializable).
        """
        from datetime import datetime
        from enum import Enum

        def to_camel(s: str) -> str:
            parts = s.split("_")
            return parts[0] + "".join(p.title() for p in parts[1:])

        result: dict[str, Any] = {}
        for k, v in kwargs.items():
            if isinstance(v, Enum):
                v = v.value
            if v is None:
                continue
            if isinstance(v, datetime):
                result[to_camel(k)] = v.isoformat()
            else:
                result[to_camel(k)] = v
        return result

    def list_products(
        self,
        account_id: str,
        *,
        limit: int | None = 20,
        cursor: str | None = None,
        status: str | None = None,
        query: str | None = None,
    ) -> dict[str, Any]:
        """List products"""
        params = self._build_params(
            limit=limit,
            cursor=cursor,
            status=status,
            query=query,
        )
        return self._client._get(f"/v1/accounts/{account_id}/products", params=params)

    def get_product(self, account_id: str, product_id: str) -> dict[str, Any]:
        """Get a product"""
        return self._client._get(f"/v1/accounts/{account_id}/products/{product_id}")

    def update_product(
        self,
        account_id: str,
        product_id: str,
        *,
        title: str | None = None,
        description_html: str | None = None,
        handle: str | None = None,
        vendor: str | None = None,
        product_type: str | None = None,
        tags: list[str] | None = None,
        status: str | None = None,
        seo: dict[str, Any] | None = None,
        variants: list[dict[str, Any]] | None = None,
    ) -> dict[str, Any]:
        """Update a product"""
        payload = self._build_payload(
            title=title,
            description_html=description_html,
            handle=handle,
            vendor=vendor,
            product_type=product_type,
            tags=tags,
            status=status,
            seo=seo,
            variants=variants,
        )
        return self._client._patch(
            f"/v1/accounts/{account_id}/products/{product_id}", data=payload
        )

    async def alist_products(
        self,
        account_id: str,
        *,
        limit: int | None = 20,
        cursor: str | None = None,
        status: str | None = None,
        query: str | None = None,
    ) -> dict[str, Any]:
        """List products (async)"""
        params = self._build_params(
            limit=limit,
            cursor=cursor,
            status=status,
            query=query,
        )
        return await self._client._aget(
            f"/v1/accounts/{account_id}/products", params=params
        )

    async def aget_product(self, account_id: str, product_id: str) -> dict[str, Any]:
        """Get a product (async)"""
        return await self._client._aget(
            f"/v1/accounts/{account_id}/products/{product_id}"
        )

    async def aupdate_product(
        self,
        account_id: str,
        product_id: str,
        *,
        title: str | None = None,
        description_html: str | None = None,
        handle: str | None = None,
        vendor: str | None = None,
        product_type: str | None = None,
        tags: list[str] | None = None,
        status: str | None = None,
        seo: dict[str, Any] | None = None,
        variants: list[dict[str, Any]] | None = None,
    ) -> dict[str, Any]:
        """Update a product (async)"""
        payload = self._build_payload(
            title=title,
            description_html=description_html,
            handle=handle,
            vendor=vendor,
            product_type=product_type,
            tags=tags,
            status=status,
            seo=seo,
            variants=variants,
        )
        return await self._client._apatch(
            f"/v1/accounts/{account_id}/products/{product_id}", data=payload
        )
