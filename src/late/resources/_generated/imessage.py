"""
Auto-generated imessage resource.

DO NOT EDIT THIS FILE MANUALLY.
Run `python scripts/generate_resources.py` to regenerate.
"""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

if TYPE_CHECKING:
    from ..client.base import BaseClient


class ImessageResource:
    """
    imessage operations.
    """

    def __init__(self, client: BaseClient) -> None:
        self._client = client

    def _build_params(self, **kwargs: Any) -> dict[str, Any]:
        """Build query parameters, filtering None and empty-string values.

        Empty strings are filtered because MCP tool wrappers pass ``""`` as the
        default for optional string args, and the API rejects empty query
        values (e.g. ``platform=``) with a 400. Filtering here keeps both direct
        SDK callers and MCP tool callers safe.

        Enum members are unwrapped to their value: httpx serializes params
        via str(), which yields "ClassName.MEMBER" for Enum members, so
        e.g. ``status=PostStatus.FAILED`` would otherwise reach the API as
        ``status=PostStatus.FAILED`` instead of ``status=failed``.
        """
        from enum import Enum

        def to_camel(s: str) -> str:
            parts = s.split("_")
            return parts[0] + "".join(p.title() for p in parts[1:])

        result: dict[str, Any] = {}
        for k, v in kwargs.items():
            if isinstance(v, Enum):
                v = v.value
            if v is None or v == "":
                continue
            result[to_camel(k)] = v
        return result

    def _build_payload(self, **kwargs: Any) -> dict[str, Any]:
        """Build request payload, filtering None values. Enum members are
        unwrapped to their value so JSON bodies carry e.g. "failed" rather
        than a raw Enum member (plain Enum members are not JSON-serializable).
        """
        from datetime import datetime
        from enum import Enum

        def to_camel(s: str) -> str:
            parts = s.split("_")
            return parts[0] + "".join(p.title() for p in parts[1:])

        result: dict[str, Any] = {}
        for k, v in kwargs.items():
            if isinstance(v, Enum):
                v = v.value
            if v is None:
                continue
            if isinstance(v, datetime):
                result[to_camel(k)] = v.isoformat()
            else:
                result[to_camel(k)] = v
        return result

    def list_imessage_senders(self) -> dict[str, Any]:
        """List iMessage senders"""
        return self._client._get("/v1/imessage/senders")

    def register_imessage_sender(
        self,
        profile_id: str,
        sender: str,
        *,
        display_name: str | None = None,
        provider: str | None = None,
    ) -> dict[str, Any]:
        """Register an iMessage sender"""
        payload = self._build_payload(
            profile_id=profile_id,
            sender=sender,
            display_name=display_name,
            provider=provider,
        )
        return self._client._post("/v1/imessage/senders", data=payload)

    def list_imessage_sender_orders(
        self, *, include_canceled: bool | None = False
    ) -> dict[str, Any]:
        """List iMessage sender orders"""
        params = self._build_params(
            include_canceled=include_canceled,
        )
        return self._client._get("/v1/imessage/senders/order", params=params)

    def order_imessage_sender(
        self,
        profile_id: str,
        kind: str,
        *,
        region: str | None = None,
        available_number_id: str | None = None,
        zip_code: str | None = None,
        email_name: str | None = None,
        email_domain: str | None = None,
        display_name: str | None = None,
        purchase_intent_id: str | None = None,
        contact: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Order a new iMessage sender"""
        payload = self._build_payload(
            profile_id=profile_id,
            kind=kind,
            region=region,
            available_number_id=available_number_id,
            zip_code=zip_code,
            email_name=email_name,
            email_domain=email_domain,
            display_name=display_name,
            purchase_intent_id=purchase_intent_id,
            contact=contact,
        )
        return self._client._post("/v1/imessage/senders/order", data=payload)

    def get_imessage_sender(self, sender_id: str) -> dict[str, Any]:
        """Get iMessage sender status"""
        return self._client._get(f"/v1/imessage/senders/{sender_id}")

    def update_imessage_sender(
        self,
        sender_id: str,
        *,
        display_name: str | None = None,
        contact: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Update an iMessage sender"""
        payload = self._build_payload(
            display_name=display_name,
            contact=contact,
        )
        return self._client._patch(f"/v1/imessage/senders/{sender_id}", data=payload)

    def cancel_imessage_sender(self, sender_id: str) -> dict[str, Any]:
        """Cancel an iMessage sender"""
        return self._client._delete(f"/v1/imessage/senders/{sender_id}")

    def list_imessage_audience(
        self,
        *,
        account_id: str | None = None,
        status: str | None = None,
        search: str | None = None,
        limit: int | None = 50,
        skip: int | None = 0,
    ) -> dict[str, Any]:
        """List iMessage audience"""
        params = self._build_params(
            account_id=account_id,
            status=status,
            search=search,
            limit=limit,
            skip=skip,
        )
        return self._client._get("/v1/imessage/audience", params=params)

    def set_imessage_subscription(
        self, account_id: str, conversation_id: str, subscribed: bool
    ) -> dict[str, Any]:
        """Subscribe or opt out an iMessage contact"""
        payload = self._build_payload(
            account_id=account_id,
            conversation_id=conversation_id,
            subscribed=subscribed,
        )
        return self._client._post("/v1/imessage/audience/subscription", data=payload)

    def create_imessage_opt_in_link(
        self,
        sender_id: str,
        body: str,
        *,
        parameters: dict[str, Any] | None = None,
        opt_in_code: str | None = None,
    ) -> dict[str, Any]:
        """Create a tracked iMessage opt-in link"""
        payload = self._build_payload(
            body=body,
            parameters=parameters,
            opt_in_code=opt_in_code,
        )
        return self._client._post(
            f"/v1/imessage/senders/{sender_id}/opt-in-links", data=payload
        )

    def list_imessage_available_numbers(
        self, *, region: str | None = None
    ) -> dict[str, Any]:
        """List instantly available iMessage numbers"""
        params = self._build_params(
            region=region,
        )
        return self._client._get(
            "/v1/imessage/senders/available-numbers", params=params
        )

    def reserve_imessage_available_number(self, number_id: str) -> dict[str, Any]:
        """Reserve an available iMessage number"""
        return self._client._post(
            f"/v1/imessage/senders/available-numbers/{number_id}/reserve"
        )

    def create_imessage_group(
        self,
        account_id: str,
        contacts: list[str],
        text: str,
        *,
        name: str | None = None,
        channel: str | None = "imessage",
    ) -> dict[str, Any]:
        """Start an iMessage group chat"""
        payload = self._build_payload(
            account_id=account_id,
            contacts=contacts,
            text=text,
            name=name,
            channel=channel,
        )
        return self._client._post("/v1/imessage/groups", data=payload)

    def get_imessage_group(
        self, conversation_id: str, account_id: str
    ) -> dict[str, Any]:
        """Get an iMessage group"""
        params = self._build_params(
            account_id=account_id,
        )
        return self._client._get(
            f"/v1/imessage/groups/{conversation_id}", params=params
        )

    def update_imessage_group(
        self,
        conversation_id: str,
        account_id: str,
        *,
        name: str | None = None,
        photo_url: str | None = None,
    ) -> dict[str, Any]:
        """Rename an iMessage group or change its photo"""
        payload = self._build_payload(
            account_id=account_id,
            name=name,
            photo_url=photo_url,
        )
        return self._client._patch(
            f"/v1/imessage/groups/{conversation_id}", data=payload
        )

    def add_imessage_group_participant(
        self, conversation_id: str, account_id: str, contact: str
    ) -> dict[str, Any]:
        """Add a participant to an iMessage group"""
        payload = self._build_payload(
            account_id=account_id,
            contact=contact,
        )
        return self._client._post(
            f"/v1/imessage/groups/{conversation_id}/participants", data=payload
        )

    def remove_imessage_group_participant(
        self, conversation_id: str, account_id: str, contact: str
    ) -> dict[str, Any]:
        """Remove a participant from an iMessage group"""
        params = self._build_params(
            account_id=account_id,
            contact=contact,
        )
        return self._client._delete(
            f"/v1/imessage/groups/{conversation_id}/participants", params=params
        )

    async def alist_imessage_senders(self) -> dict[str, Any]:
        """List iMessage senders (async)"""
        return await self._client._aget("/v1/imessage/senders")

    async def aregister_imessage_sender(
        self,
        profile_id: str,
        sender: str,
        *,
        display_name: str | None = None,
        provider: str | None = None,
    ) -> dict[str, Any]:
        """Register an iMessage sender (async)"""
        payload = self._build_payload(
            profile_id=profile_id,
            sender=sender,
            display_name=display_name,
            provider=provider,
        )
        return await self._client._apost("/v1/imessage/senders", data=payload)

    async def alist_imessage_sender_orders(
        self, *, include_canceled: bool | None = False
    ) -> dict[str, Any]:
        """List iMessage sender orders (async)"""
        params = self._build_params(
            include_canceled=include_canceled,
        )
        return await self._client._aget("/v1/imessage/senders/order", params=params)

    async def aorder_imessage_sender(
        self,
        profile_id: str,
        kind: str,
        *,
        region: str | None = None,
        available_number_id: str | None = None,
        zip_code: str | None = None,
        email_name: str | None = None,
        email_domain: str | None = None,
        display_name: str | None = None,
        purchase_intent_id: str | None = None,
        contact: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Order a new iMessage sender (async)"""
        payload = self._build_payload(
            profile_id=profile_id,
            kind=kind,
            region=region,
            available_number_id=available_number_id,
            zip_code=zip_code,
            email_name=email_name,
            email_domain=email_domain,
            display_name=display_name,
            purchase_intent_id=purchase_intent_id,
            contact=contact,
        )
        return await self._client._apost("/v1/imessage/senders/order", data=payload)

    async def aget_imessage_sender(self, sender_id: str) -> dict[str, Any]:
        """Get iMessage sender status (async)"""
        return await self._client._aget(f"/v1/imessage/senders/{sender_id}")

    async def aupdate_imessage_sender(
        self,
        sender_id: str,
        *,
        display_name: str | None = None,
        contact: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Update an iMessage sender (async)"""
        payload = self._build_payload(
            display_name=display_name,
            contact=contact,
        )
        return await self._client._apatch(
            f"/v1/imessage/senders/{sender_id}", data=payload
        )

    async def acancel_imessage_sender(self, sender_id: str) -> dict[str, Any]:
        """Cancel an iMessage sender (async)"""
        return await self._client._adelete(f"/v1/imessage/senders/{sender_id}")

    async def alist_imessage_audience(
        self,
        *,
        account_id: str | None = None,
        status: str | None = None,
        search: str | None = None,
        limit: int | None = 50,
        skip: int | None = 0,
    ) -> dict[str, Any]:
        """List iMessage audience (async)"""
        params = self._build_params(
            account_id=account_id,
            status=status,
            search=search,
            limit=limit,
            skip=skip,
        )
        return await self._client._aget("/v1/imessage/audience", params=params)

    async def aset_imessage_subscription(
        self, account_id: str, conversation_id: str, subscribed: bool
    ) -> dict[str, Any]:
        """Subscribe or opt out an iMessage contact (async)"""
        payload = self._build_payload(
            account_id=account_id,
            conversation_id=conversation_id,
            subscribed=subscribed,
        )
        return await self._client._apost(
            "/v1/imessage/audience/subscription", data=payload
        )

    async def acreate_imessage_opt_in_link(
        self,
        sender_id: str,
        body: str,
        *,
        parameters: dict[str, Any] | None = None,
        opt_in_code: str | None = None,
    ) -> dict[str, Any]:
        """Create a tracked iMessage opt-in link (async)"""
        payload = self._build_payload(
            body=body,
            parameters=parameters,
            opt_in_code=opt_in_code,
        )
        return await self._client._apost(
            f"/v1/imessage/senders/{sender_id}/opt-in-links", data=payload
        )

    async def alist_imessage_available_numbers(
        self, *, region: str | None = None
    ) -> dict[str, Any]:
        """List instantly available iMessage numbers (async)"""
        params = self._build_params(
            region=region,
        )
        return await self._client._aget(
            "/v1/imessage/senders/available-numbers", params=params
        )

    async def areserve_imessage_available_number(
        self, number_id: str
    ) -> dict[str, Any]:
        """Reserve an available iMessage number (async)"""
        return await self._client._apost(
            f"/v1/imessage/senders/available-numbers/{number_id}/reserve"
        )

    async def acreate_imessage_group(
        self,
        account_id: str,
        contacts: list[str],
        text: str,
        *,
        name: str | None = None,
        channel: str | None = "imessage",
    ) -> dict[str, Any]:
        """Start an iMessage group chat (async)"""
        payload = self._build_payload(
            account_id=account_id,
            contacts=contacts,
            text=text,
            name=name,
            channel=channel,
        )
        return await self._client._apost("/v1/imessage/groups", data=payload)

    async def aget_imessage_group(
        self, conversation_id: str, account_id: str
    ) -> dict[str, Any]:
        """Get an iMessage group (async)"""
        params = self._build_params(
            account_id=account_id,
        )
        return await self._client._aget(
            f"/v1/imessage/groups/{conversation_id}", params=params
        )

    async def aupdate_imessage_group(
        self,
        conversation_id: str,
        account_id: str,
        *,
        name: str | None = None,
        photo_url: str | None = None,
    ) -> dict[str, Any]:
        """Rename an iMessage group or change its photo (async)"""
        payload = self._build_payload(
            account_id=account_id,
            name=name,
            photo_url=photo_url,
        )
        return await self._client._apatch(
            f"/v1/imessage/groups/{conversation_id}", data=payload
        )

    async def aadd_imessage_group_participant(
        self, conversation_id: str, account_id: str, contact: str
    ) -> dict[str, Any]:
        """Add a participant to an iMessage group (async)"""
        payload = self._build_payload(
            account_id=account_id,
            contact=contact,
        )
        return await self._client._apost(
            f"/v1/imessage/groups/{conversation_id}/participants", data=payload
        )

    async def aremove_imessage_group_participant(
        self, conversation_id: str, account_id: str, contact: str
    ) -> dict[str, Any]:
        """Remove a participant from an iMessage group (async)"""
        params = self._build_params(
            account_id=account_id,
            contact=contact,
        )
        return await self._client._adelete(
            f"/v1/imessage/groups/{conversation_id}/participants", params=params
        )
