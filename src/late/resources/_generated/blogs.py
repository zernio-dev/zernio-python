"""
Auto-generated blogs resource.

DO NOT EDIT THIS FILE MANUALLY.
Run `python scripts/generate_resources.py` to regenerate.
"""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

if TYPE_CHECKING:
    from datetime import datetime

    from ..client.base import BaseClient


class BlogsResource:
    """
    blogs operations.
    """

    def __init__(self, client: BaseClient) -> None:
        self._client = client

    def _build_params(self, **kwargs: Any) -> dict[str, Any]:
        """Build query parameters, filtering None and empty-string values.

        Empty strings are filtered because MCP tool wrappers pass ``""`` as the
        default for optional string args, and the API rejects empty query
        values (e.g. ``platform=``) with a 400. Filtering here keeps both direct
        SDK callers and MCP tool callers safe.

        Enum members are unwrapped to their value: httpx serializes params
        via str(), which yields "ClassName.MEMBER" for Enum members, so
        e.g. ``status=PostStatus.FAILED`` would otherwise reach the API as
        ``status=PostStatus.FAILED`` instead of ``status=failed``.
        """
        from enum import Enum

        def to_camel(s: str) -> str:
            parts = s.split("_")
            return parts[0] + "".join(p.title() for p in parts[1:])

        result: dict[str, Any] = {}
        for k, v in kwargs.items():
            if isinstance(v, Enum):
                v = v.value
            if v is None or v == "":
                continue
            result[to_camel(k)] = v
        return result

    def _build_payload(self, **kwargs: Any) -> dict[str, Any]:
        """Build request payload, filtering None values. Enum members are
        unwrapped to their value so JSON bodies carry e.g. "failed" rather
        than a raw Enum member (plain Enum members are not JSON-serializable).
        """
        from datetime import datetime
        from enum import Enum

        def to_camel(s: str) -> str:
            parts = s.split("_")
            return parts[0] + "".join(p.title() for p in parts[1:])

        result: dict[str, Any] = {}
        for k, v in kwargs.items():
            if isinstance(v, Enum):
                v = v.value
            if v is None:
                continue
            if isinstance(v, datetime):
                result[to_camel(k)] = v.isoformat()
            else:
                result[to_camel(k)] = v
        return result

    def list_blogs(
        self, account_id: str, *, limit: int | None = 20, cursor: str | None = None
    ) -> dict[str, Any]:
        """List blogs"""
        params = self._build_params(
            limit=limit,
            cursor=cursor,
        )
        return self._client._get(f"/v1/accounts/{account_id}/blogs", params=params)

    def create_blog(
        self, account_id: str, title: str, *, handle: str | None = None
    ) -> dict[str, Any]:
        """Create a blog"""
        payload = self._build_payload(
            title=title,
            handle=handle,
        )
        return self._client._post(f"/v1/accounts/{account_id}/blogs", data=payload)

    def get_blog(self, account_id: str, blog_id: str) -> dict[str, Any]:
        """Get a blog"""
        return self._client._get(f"/v1/accounts/{account_id}/blogs/{blog_id}")

    def update_blog(
        self,
        account_id: str,
        blog_id: str,
        *,
        title: str | None = None,
        handle: str | None = None,
    ) -> dict[str, Any]:
        """Update a blog"""
        payload = self._build_payload(
            title=title,
            handle=handle,
        )
        return self._client._patch(
            f"/v1/accounts/{account_id}/blogs/{blog_id}", data=payload
        )

    def delete_blog(self, account_id: str, blog_id: str) -> dict[str, Any]:
        """Delete a blog"""
        return self._client._delete(f"/v1/accounts/{account_id}/blogs/{blog_id}")

    def list_blog_articles(
        self,
        account_id: str,
        blog_id: str,
        *,
        limit: int | None = 20,
        cursor: str | None = None,
    ) -> dict[str, Any]:
        """List blog articles"""
        params = self._build_params(
            limit=limit,
            cursor=cursor,
        )
        return self._client._get(
            f"/v1/accounts/{account_id}/blogs/{blog_id}/articles", params=params
        )

    def create_blog_article(
        self,
        account_id: str,
        blog_id: str,
        title: str,
        *,
        body_html: str | None = None,
        handle: str | None = None,
        tags: list[str] | None = None,
        author: str | None = None,
        excerpt: str | None = None,
        image: dict[str, Any] | None = None,
        seo: dict[str, Any] | None = None,
        is_published: bool | None = None,
        publish_date: datetime | str | None = None,
    ) -> dict[str, Any]:
        """Create a blog article"""
        payload = self._build_payload(
            title=title,
            body_html=body_html,
            handle=handle,
            tags=tags,
            author=author,
            excerpt=excerpt,
            image=image,
            seo=seo,
            is_published=is_published,
            publish_date=publish_date,
        )
        return self._client._post(
            f"/v1/accounts/{account_id}/blogs/{blog_id}/articles", data=payload
        )

    def get_blog_article(
        self, account_id: str, blog_id: str, article_id: str
    ) -> dict[str, Any]:
        """Get a blog article"""
        return self._client._get(
            f"/v1/accounts/{account_id}/blogs/{blog_id}/articles/{article_id}"
        )

    def update_blog_article(
        self,
        account_id: str,
        blog_id: str,
        article_id: str,
        *,
        title: str | None = None,
        body_html: str | None = None,
        handle: str | None = None,
        tags: list[str] | None = None,
        author: str | None = None,
        excerpt: str | None = None,
        image: dict[str, Any] | None = None,
        seo: dict[str, Any] | None = None,
        is_published: bool | None = None,
        publish_date: datetime | str | None = None,
    ) -> dict[str, Any]:
        """Update a blog article"""
        payload = self._build_payload(
            title=title,
            body_html=body_html,
            handle=handle,
            tags=tags,
            author=author,
            excerpt=excerpt,
            image=image,
            seo=seo,
            is_published=is_published,
            publish_date=publish_date,
        )
        return self._client._patch(
            f"/v1/accounts/{account_id}/blogs/{blog_id}/articles/{article_id}",
            data=payload,
        )

    def delete_blog_article(
        self, account_id: str, blog_id: str, article_id: str
    ) -> dict[str, Any]:
        """Delete a blog article"""
        return self._client._delete(
            f"/v1/accounts/{account_id}/blogs/{blog_id}/articles/{article_id}"
        )

    async def alist_blogs(
        self, account_id: str, *, limit: int | None = 20, cursor: str | None = None
    ) -> dict[str, Any]:
        """List blogs (async)"""
        params = self._build_params(
            limit=limit,
            cursor=cursor,
        )
        return await self._client._aget(
            f"/v1/accounts/{account_id}/blogs", params=params
        )

    async def acreate_blog(
        self, account_id: str, title: str, *, handle: str | None = None
    ) -> dict[str, Any]:
        """Create a blog (async)"""
        payload = self._build_payload(
            title=title,
            handle=handle,
        )
        return await self._client._apost(
            f"/v1/accounts/{account_id}/blogs", data=payload
        )

    async def aget_blog(self, account_id: str, blog_id: str) -> dict[str, Any]:
        """Get a blog (async)"""
        return await self._client._aget(f"/v1/accounts/{account_id}/blogs/{blog_id}")

    async def aupdate_blog(
        self,
        account_id: str,
        blog_id: str,
        *,
        title: str | None = None,
        handle: str | None = None,
    ) -> dict[str, Any]:
        """Update a blog (async)"""
        payload = self._build_payload(
            title=title,
            handle=handle,
        )
        return await self._client._apatch(
            f"/v1/accounts/{account_id}/blogs/{blog_id}", data=payload
        )

    async def adelete_blog(self, account_id: str, blog_id: str) -> dict[str, Any]:
        """Delete a blog (async)"""
        return await self._client._adelete(f"/v1/accounts/{account_id}/blogs/{blog_id}")

    async def alist_blog_articles(
        self,
        account_id: str,
        blog_id: str,
        *,
        limit: int | None = 20,
        cursor: str | None = None,
    ) -> dict[str, Any]:
        """List blog articles (async)"""
        params = self._build_params(
            limit=limit,
            cursor=cursor,
        )
        return await self._client._aget(
            f"/v1/accounts/{account_id}/blogs/{blog_id}/articles", params=params
        )

    async def acreate_blog_article(
        self,
        account_id: str,
        blog_id: str,
        title: str,
        *,
        body_html: str | None = None,
        handle: str | None = None,
        tags: list[str] | None = None,
        author: str | None = None,
        excerpt: str | None = None,
        image: dict[str, Any] | None = None,
        seo: dict[str, Any] | None = None,
        is_published: bool | None = None,
        publish_date: datetime | str | None = None,
    ) -> dict[str, Any]:
        """Create a blog article (async)"""
        payload = self._build_payload(
            title=title,
            body_html=body_html,
            handle=handle,
            tags=tags,
            author=author,
            excerpt=excerpt,
            image=image,
            seo=seo,
            is_published=is_published,
            publish_date=publish_date,
        )
        return await self._client._apost(
            f"/v1/accounts/{account_id}/blogs/{blog_id}/articles", data=payload
        )

    async def aget_blog_article(
        self, account_id: str, blog_id: str, article_id: str
    ) -> dict[str, Any]:
        """Get a blog article (async)"""
        return await self._client._aget(
            f"/v1/accounts/{account_id}/blogs/{blog_id}/articles/{article_id}"
        )

    async def aupdate_blog_article(
        self,
        account_id: str,
        blog_id: str,
        article_id: str,
        *,
        title: str | None = None,
        body_html: str | None = None,
        handle: str | None = None,
        tags: list[str] | None = None,
        author: str | None = None,
        excerpt: str | None = None,
        image: dict[str, Any] | None = None,
        seo: dict[str, Any] | None = None,
        is_published: bool | None = None,
        publish_date: datetime | str | None = None,
    ) -> dict[str, Any]:
        """Update a blog article (async)"""
        payload = self._build_payload(
            title=title,
            body_html=body_html,
            handle=handle,
            tags=tags,
            author=author,
            excerpt=excerpt,
            image=image,
            seo=seo,
            is_published=is_published,
            publish_date=publish_date,
        )
        return await self._client._apatch(
            f"/v1/accounts/{account_id}/blogs/{blog_id}/articles/{article_id}",
            data=payload,
        )

    async def adelete_blog_article(
        self, account_id: str, blog_id: str, article_id: str
    ) -> dict[str, Any]:
        """Delete a blog article (async)"""
        return await self._client._adelete(
            f"/v1/accounts/{account_id}/blogs/{blog_id}/articles/{article_id}"
        )
