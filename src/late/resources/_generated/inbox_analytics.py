"""
Auto-generated inbox_analytics resource.

DO NOT EDIT THIS FILE MANUALLY.
Run `python scripts/generate_resources.py` to regenerate.
"""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

if TYPE_CHECKING:
    from ..client.base import BaseClient


class InboxAnalyticsResource:
    """
    inbox_analytics operations.
    """

    def __init__(self, client: BaseClient) -> None:
        self._client = client

    def _build_params(self, **kwargs: Any) -> dict[str, Any]:
        """Build query parameters, filtering None and empty-string values.

        Empty strings are filtered because MCP tool wrappers pass ``""`` as the
        default for optional string args, and the API rejects empty query
        values (e.g. ``platform=``) with a 400. Filtering here keeps both direct
        SDK callers and MCP tool callers safe.
        """

        def to_camel(s: str) -> str:
            parts = s.split("_")
            return parts[0] + "".join(p.title() for p in parts[1:])

        return {to_camel(k): v for k, v in kwargs.items() if v is not None and v != ""}

    def _build_payload(self, **kwargs: Any) -> dict[str, Any]:
        """Build request payload, filtering None values."""
        from datetime import datetime

        def to_camel(s: str) -> str:
            parts = s.split("_")
            return parts[0] + "".join(p.title() for p in parts[1:])

        result: dict[str, Any] = {}
        for k, v in kwargs.items():
            if v is None:
                continue
            if isinstance(v, datetime):
                result[to_camel(k)] = v.isoformat()
            else:
                result[to_camel(k)] = v
        return result

    def get_inbox_volume(
        self,
        from_date: str,
        *,
        to_date: str | None = None,
        profile_id: str | None = None,
        platform: str | None = None,
        account_id: str | None = None,
        source: str | None = None,
    ) -> dict[str, Any]:
        """Get inbox messaging volume"""
        params = self._build_params(
            from_date=from_date,
            to_date=to_date,
            profile_id=profile_id,
            platform=platform,
            account_id=account_id,
            source=source,
        )
        return self._client._get("/v1/analytics/inbox/volume", params=params)

    def get_inbox_heatmap(
        self,
        from_date: str,
        *,
        to_date: str | None = None,
        profile_id: str | None = None,
        platform: str | None = None,
        account_id: str | None = None,
        source: str | None = None,
        action: str | None = None,
    ) -> dict[str, Any]:
        """Get inbox day-of-week × hour-of-day heatmap"""
        params = self._build_params(
            from_date=from_date,
            to_date=to_date,
            profile_id=profile_id,
            platform=platform,
            account_id=account_id,
            source=source,
            action=action,
        )
        return self._client._get("/v1/analytics/inbox/heatmap", params=params)

    def get_inbox_source_breakdown(
        self,
        from_date: str,
        *,
        to_date: str | None = None,
        profile_id: str | None = None,
        platform: str | None = None,
        account_id: str | None = None,
    ) -> dict[str, Any]:
        """Get inbox source breakdown"""
        params = self._build_params(
            from_date=from_date,
            to_date=to_date,
            profile_id=profile_id,
            platform=platform,
            account_id=account_id,
        )
        return self._client._get("/v1/analytics/inbox/source-breakdown", params=params)

    def get_inbox_response_time(
        self,
        from_date: str,
        *,
        to_date: str | None = None,
        profile_id: str | None = None,
        platform: str | None = None,
        account_id: str | None = None,
    ) -> dict[str, Any]:
        """Get inbox response-time stats"""
        params = self._build_params(
            from_date=from_date,
            to_date=to_date,
            profile_id=profile_id,
            platform=platform,
            account_id=account_id,
        )
        return self._client._get("/v1/analytics/inbox/response-time", params=params)

    def get_inbox_top_accounts(
        self,
        from_date: str,
        *,
        to_date: str | None = None,
        profile_id: str | None = None,
        platform: str | None = None,
        source: str | None = None,
        limit: int | None = 10,
    ) -> dict[str, Any]:
        """Get top accounts by inbox volume"""
        params = self._build_params(
            from_date=from_date,
            to_date=to_date,
            profile_id=profile_id,
            platform=platform,
            source=source,
            limit=limit,
        )
        return self._client._get("/v1/analytics/inbox/top-accounts", params=params)

    def list_inbox_conversation_analytics(
        self,
        from_date: str,
        *,
        to_date: str | None = None,
        profile_id: str | None = None,
        platform: str | None = None,
        account_id: str | None = None,
        source: str | None = None,
        limit: int | None = 50,
        page: int | None = 1,
        sort_by: str | None = "lastMessageAt",
        order: str | None = "desc",
    ) -> dict[str, Any]:
        """List conversations with inbox analytics"""
        params = self._build_params(
            from_date=from_date,
            to_date=to_date,
            profile_id=profile_id,
            platform=platform,
            account_id=account_id,
            source=source,
            limit=limit,
            page=page,
            sort_by=sort_by,
            order=order,
        )
        return self._client._get("/v1/analytics/inbox/conversations", params=params)

    def get_inbox_conversation_analytics(
        self, conversation_id: str, from_date: str, *, to_date: str | None = None
    ) -> dict[str, Any]:
        """Get analytics for a single conversation"""
        params = self._build_params(
            from_date=from_date,
            to_date=to_date,
        )
        return self._client._get(
            f"/v1/analytics/inbox/conversations/{conversation_id}", params=params
        )

    async def aget_inbox_volume(
        self,
        from_date: str,
        *,
        to_date: str | None = None,
        profile_id: str | None = None,
        platform: str | None = None,
        account_id: str | None = None,
        source: str | None = None,
    ) -> dict[str, Any]:
        """Get inbox messaging volume (async)"""
        params = self._build_params(
            from_date=from_date,
            to_date=to_date,
            profile_id=profile_id,
            platform=platform,
            account_id=account_id,
            source=source,
        )
        return await self._client._aget("/v1/analytics/inbox/volume", params=params)

    async def aget_inbox_heatmap(
        self,
        from_date: str,
        *,
        to_date: str | None = None,
        profile_id: str | None = None,
        platform: str | None = None,
        account_id: str | None = None,
        source: str | None = None,
        action: str | None = None,
    ) -> dict[str, Any]:
        """Get inbox day-of-week × hour-of-day heatmap (async)"""
        params = self._build_params(
            from_date=from_date,
            to_date=to_date,
            profile_id=profile_id,
            platform=platform,
            account_id=account_id,
            source=source,
            action=action,
        )
        return await self._client._aget("/v1/analytics/inbox/heatmap", params=params)

    async def aget_inbox_source_breakdown(
        self,
        from_date: str,
        *,
        to_date: str | None = None,
        profile_id: str | None = None,
        platform: str | None = None,
        account_id: str | None = None,
    ) -> dict[str, Any]:
        """Get inbox source breakdown (async)"""
        params = self._build_params(
            from_date=from_date,
            to_date=to_date,
            profile_id=profile_id,
            platform=platform,
            account_id=account_id,
        )
        return await self._client._aget(
            "/v1/analytics/inbox/source-breakdown", params=params
        )

    async def aget_inbox_response_time(
        self,
        from_date: str,
        *,
        to_date: str | None = None,
        profile_id: str | None = None,
        platform: str | None = None,
        account_id: str | None = None,
    ) -> dict[str, Any]:
        """Get inbox response-time stats (async)"""
        params = self._build_params(
            from_date=from_date,
            to_date=to_date,
            profile_id=profile_id,
            platform=platform,
            account_id=account_id,
        )
        return await self._client._aget(
            "/v1/analytics/inbox/response-time", params=params
        )

    async def aget_inbox_top_accounts(
        self,
        from_date: str,
        *,
        to_date: str | None = None,
        profile_id: str | None = None,
        platform: str | None = None,
        source: str | None = None,
        limit: int | None = 10,
    ) -> dict[str, Any]:
        """Get top accounts by inbox volume (async)"""
        params = self._build_params(
            from_date=from_date,
            to_date=to_date,
            profile_id=profile_id,
            platform=platform,
            source=source,
            limit=limit,
        )
        return await self._client._aget(
            "/v1/analytics/inbox/top-accounts", params=params
        )

    async def alist_inbox_conversation_analytics(
        self,
        from_date: str,
        *,
        to_date: str | None = None,
        profile_id: str | None = None,
        platform: str | None = None,
        account_id: str | None = None,
        source: str | None = None,
        limit: int | None = 50,
        page: int | None = 1,
        sort_by: str | None = "lastMessageAt",
        order: str | None = "desc",
    ) -> dict[str, Any]:
        """List conversations with inbox analytics (async)"""
        params = self._build_params(
            from_date=from_date,
            to_date=to_date,
            profile_id=profile_id,
            platform=platform,
            account_id=account_id,
            source=source,
            limit=limit,
            page=page,
            sort_by=sort_by,
            order=order,
        )
        return await self._client._aget(
            "/v1/analytics/inbox/conversations", params=params
        )

    async def aget_inbox_conversation_analytics(
        self, conversation_id: str, from_date: str, *, to_date: str | None = None
    ) -> dict[str, Any]:
        """Get analytics for a single conversation (async)"""
        params = self._build_params(
            from_date=from_date,
            to_date=to_date,
        )
        return await self._client._aget(
            f"/v1/analytics/inbox/conversations/{conversation_id}", params=params
        )
