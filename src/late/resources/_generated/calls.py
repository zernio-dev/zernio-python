"""
Auto-generated calls resource.

DO NOT EDIT THIS FILE MANUALLY.
Run `python scripts/generate_resources.py` to regenerate.
"""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

if TYPE_CHECKING:
    from datetime import datetime

    from ..client.base import BaseClient


class CallsResource:
    """
    calls operations.
    """

    def __init__(self, client: BaseClient) -> None:
        self._client = client

    def _build_params(self, **kwargs: Any) -> dict[str, Any]:
        """Build query parameters, filtering None and empty-string values.

        Empty strings are filtered because MCP tool wrappers pass ``""`` as the
        default for optional string args, and the API rejects empty query
        values (e.g. ``platform=``) with a 400. Filtering here keeps both direct
        SDK callers and MCP tool callers safe.
        """

        def to_camel(s: str) -> str:
            parts = s.split("_")
            return parts[0] + "".join(p.title() for p in parts[1:])

        return {to_camel(k): v for k, v in kwargs.items() if v is not None and v != ""}

    def _build_payload(self, **kwargs: Any) -> dict[str, Any]:
        """Build request payload, filtering None values."""
        from datetime import datetime

        def to_camel(s: str) -> str:
            parts = s.split("_")
            return parts[0] + "".join(p.title() for p in parts[1:])

        result: dict[str, Any] = {}
        for k, v in kwargs.items():
            if v is None:
                continue
            if isinstance(v, datetime):
                result[to_camel(k)] = v.isoformat()
            else:
                result[to_camel(k)] = v
        return result

    def list_calls(
        self,
        *,
        channel: str | None = None,
        status: str | None = None,
        direction: str | None = None,
        number: str | None = None,
        search: str | None = None,
        before: datetime | str | None = None,
        limit: int | None = 50,
    ) -> dict[str, Any]:
        """List all calls (unified history)"""
        params = self._build_params(
            channel=channel,
            status=status,
            direction=direction,
            number=number,
            search=search,
            before=before,
            limit=limit,
        )
        return self._client._get("/v1/calls", params=params)

    def get_call(self, id: str) -> dict[str, Any]:
        """Get a call (any channel)"""
        return self._client._get(f"/v1/calls/{id}")

    def get_call_recording(self, id: str, *, as_: str | None = None) -> dict[str, Any]:
        """Get a call recording"""
        params = self._build_params(
            as_=as_,
        )
        return self._client._get(f"/v1/calls/{id}/recording", params=params)

    async def alist_calls(
        self,
        *,
        channel: str | None = None,
        status: str | None = None,
        direction: str | None = None,
        number: str | None = None,
        search: str | None = None,
        before: datetime | str | None = None,
        limit: int | None = 50,
    ) -> dict[str, Any]:
        """List all calls (unified history) (async)"""
        params = self._build_params(
            channel=channel,
            status=status,
            direction=direction,
            number=number,
            search=search,
            before=before,
            limit=limit,
        )
        return await self._client._aget("/v1/calls", params=params)

    async def aget_call(self, id: str) -> dict[str, Any]:
        """Get a call (any channel) (async)"""
        return await self._client._aget(f"/v1/calls/{id}")

    async def aget_call_recording(
        self, id: str, *, as_: str | None = None
    ) -> dict[str, Any]:
        """Get a call recording (async)"""
        params = self._build_params(
            as_=as_,
        )
        return await self._client._aget(f"/v1/calls/{id}/recording", params=params)
