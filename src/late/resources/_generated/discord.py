"""
Auto-generated discord resource.

DO NOT EDIT THIS FILE MANUALLY.
Run `python scripts/generate_resources.py` to regenerate.
"""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

if TYPE_CHECKING:
    from ..client.base import BaseClient


class DiscordResource:
    """
    discord operations.
    """

    def __init__(self, client: BaseClient) -> None:
        self._client = client

    def _build_params(self, **kwargs: Any) -> dict[str, Any]:
        """Build query parameters, filtering None values."""

        def to_camel(s: str) -> str:
            parts = s.split("_")
            return parts[0] + "".join(p.title() for p in parts[1:])

        return {to_camel(k): v for k, v in kwargs.items() if v is not None}

    def _build_payload(self, **kwargs: Any) -> dict[str, Any]:
        """Build request payload, filtering None values."""
        from datetime import datetime

        def to_camel(s: str) -> str:
            parts = s.split("_")
            return parts[0] + "".join(p.title() for p in parts[1:])

        result: dict[str, Any] = {}
        for k, v in kwargs.items():
            if v is None:
                continue
            if isinstance(v, datetime):
                result[to_camel(k)] = v.isoformat()
            else:
                result[to_camel(k)] = v
        return result

    def get_discord_settings(self, account_id: str) -> dict[str, Any]:
        """Get Discord account settings"""
        return self._client._get(f"/v1/accounts/{account_id}/discord-settings")

    def update_discord_settings(
        self,
        account_id: str,
        *,
        webhook_username: str | None = None,
        webhook_avatar_url: str | None = None,
        channel_id: str | None = None,
    ) -> dict[str, Any]:
        """Update Discord settings"""
        payload = self._build_payload(
            webhook_username=webhook_username,
            webhook_avatar_url=webhook_avatar_url,
            channel_id=channel_id,
        )
        return self._client._patch(
            f"/v1/accounts/{account_id}/discord-settings", data=payload
        )

    def get_discord_channels(self, account_id: str) -> dict[str, Any]:
        """List Discord guild channels"""
        return self._client._get(f"/v1/accounts/{account_id}/discord-channels")

    async def aget_discord_settings(self, account_id: str) -> dict[str, Any]:
        """Get Discord account settings (async)"""
        return await self._client._aget(f"/v1/accounts/{account_id}/discord-settings")

    async def aupdate_discord_settings(
        self,
        account_id: str,
        *,
        webhook_username: str | None = None,
        webhook_avatar_url: str | None = None,
        channel_id: str | None = None,
    ) -> dict[str, Any]:
        """Update Discord settings (async)"""
        payload = self._build_payload(
            webhook_username=webhook_username,
            webhook_avatar_url=webhook_avatar_url,
            channel_id=channel_id,
        )
        return await self._client._apatch(
            f"/v1/accounts/{account_id}/discord-settings", data=payload
        )

    async def aget_discord_channels(self, account_id: str) -> dict[str, Any]:
        """List Discord guild channels (async)"""
        return await self._client._aget(f"/v1/accounts/{account_id}/discord-channels")
