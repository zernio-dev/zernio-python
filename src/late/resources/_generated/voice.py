"""
Auto-generated voice resource.

DO NOT EDIT THIS FILE MANUALLY.
Run `python scripts/generate_resources.py` to regenerate.
"""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

if TYPE_CHECKING:
    from datetime import datetime

    from ..client.base import BaseClient


class VoiceResource:
    """
    voice operations.
    """

    def __init__(self, client: BaseClient) -> None:
        self._client = client

    def _build_params(self, **kwargs: Any) -> dict[str, Any]:
        """Build query parameters, filtering None and empty-string values.

        Empty strings are filtered because MCP tool wrappers pass ``""`` as the
        default for optional string args, and the API rejects empty query
        values (e.g. ``platform=``) with a 400. Filtering here keeps both direct
        SDK callers and MCP tool callers safe.

        Enum members are unwrapped to their value: httpx serializes params
        via str(), which yields "ClassName.MEMBER" for Enum members, so
        e.g. ``status=PostStatus.FAILED`` would otherwise reach the API as
        ``status=PostStatus.FAILED`` instead of ``status=failed``.
        """
        from enum import Enum

        def to_camel(s: str) -> str:
            parts = s.split("_")
            return parts[0] + "".join(p.title() for p in parts[1:])

        result: dict[str, Any] = {}
        for k, v in kwargs.items():
            if isinstance(v, Enum):
                v = v.value
            if v is None or v == "":
                continue
            result[to_camel(k)] = v
        return result

    def _build_payload(self, **kwargs: Any) -> dict[str, Any]:
        """Build request payload, filtering None values. Enum members are
        unwrapped to their value so JSON bodies carry e.g. "failed" rather
        than a raw Enum member (plain Enum members are not JSON-serializable).
        """
        from datetime import datetime
        from enum import Enum

        def to_camel(s: str) -> str:
            parts = s.split("_")
            return parts[0] + "".join(p.title() for p in parts[1:])

        result: dict[str, Any] = {}
        for k, v in kwargs.items():
            if isinstance(v, Enum):
                v = v.value
            if v is None:
                continue
            if isinstance(v, datetime):
                result[to_camel(k)] = v.isoformat()
            else:
                result[to_camel(k)] = v
        return result

    def create_voice_call(
        self,
        to: str,
        *,
        from_number: str | None = None,
        forward_to: str | None = None,
        greeting: str | None = None,
        record_override: bool | None = None,
        transcribe_override: bool | None = None,
        transcription_language: str | None = None,
        amd: bool | None = None,
        voicemail_drop_message: str | None = None,
    ) -> dict[str, Any]:
        """Place an outbound phone call"""
        payload = self._build_payload(
            to=to,
            from_number=from_number,
            forward_to=forward_to,
            greeting=greeting,
            record_override=record_override,
            transcribe_override=transcribe_override,
            transcription_language=transcription_language,
            amd=amd,
            voicemail_drop_message=voicemail_drop_message,
        )
        return self._client._post("/v1/voice/calls", data=payload)

    def list_voice_calls(
        self,
        *,
        status: str | None = None,
        direction: str | None = None,
        number: str | None = None,
        before: datetime | str | None = None,
        limit: int | None = 50,
    ) -> dict[str, Any]:
        """List phone calls"""
        params = self._build_params(
            status=status,
            direction=direction,
            number=number,
            before=before,
            limit=limit,
        )
        return self._client._get("/v1/voice/calls", params=params)

    def get_voice_call(self, id: str) -> dict[str, Any]:
        """Get a phone call"""
        return self._client._get(f"/v1/voice/calls/{id}")

    def end_voice_call(self, id: str) -> dict[str, Any]:
        """Hang up a live call"""
        return self._client._post(f"/v1/voice/calls/{id}/end")

    def get_voice_call_recording(
        self, id: str, *, as_: str | None = None
    ) -> dict[str, Any]:
        """Get a call recording"""
        params = self._build_params(
            as_=as_,
        )
        return self._client._get(f"/v1/voice/calls/{id}/recording", params=params)

    def transfer_voice_call(self, id: str, to: str) -> dict[str, Any]:
        """Blind-transfer a live call"""
        payload = self._build_payload(
            to=to,
        )
        return self._client._post(f"/v1/voice/calls/{id}/transfer", data=payload)

    def get_voice_call_estimate(
        self,
        to: str,
        *,
        minutes: int | None = 1,
        recording: bool | None = None,
        transcription: bool | None = None,
    ) -> dict[str, Any]:
        """Estimate call cost"""
        params = self._build_params(
            to=to,
            minutes=minutes,
            recording=recording,
            transcription=transcription,
        )
        return self._client._get("/v1/voice/calls/estimate", params=params)

    def create_voice_web_session(self) -> dict[str, Any]:
        """Mint a browser softphone session"""
        return self._client._post("/v1/voice/calls/web")

    def dial_voice_web_call(
        self,
        to: str,
        credential_id: str,
        *,
        from_number: str | None = None,
        record_override: bool | None = None,
    ) -> dict[str, Any]:
        """Dial from the browser softphone"""
        payload = self._build_payload(
            to=to,
            credential_id=credential_id,
            from_number=from_number,
            record_override=record_override,
        )
        return self._client._post("/v1/voice/calls/web/dial", data=payload)

    def enable_voice_on_number(
        self,
        id: str,
        *,
        forward_to: str | None = None,
        recording_enabled: bool | None = None,
        transcription_enabled: bool | None = None,
        transcription_language: str | None = None,
        voicemail_enabled: bool | None = None,
        voicemail_greeting: str | None = None,
        business_hours_enabled: bool | None = None,
        business_hours_timezone: str | None = None,
        business_hours: list[dict[str, Any]] | None = None,
        blocked_callers: list[str] | None = None,
        forward_caller_id: str | None = None,
        ivr_enabled: bool | None = None,
        ivr_prompt: str | None = None,
        ivr_options: list[dict[str, Any]] | None = None,
    ) -> dict[str, Any]:
        """Enable phone calling on a number"""
        payload = self._build_payload(
            forward_to=forward_to,
            recording_enabled=recording_enabled,
            transcription_enabled=transcription_enabled,
            transcription_language=transcription_language,
            voicemail_enabled=voicemail_enabled,
            voicemail_greeting=voicemail_greeting,
            business_hours_enabled=business_hours_enabled,
            business_hours_timezone=business_hours_timezone,
            business_hours=business_hours,
            blocked_callers=blocked_callers,
            forward_caller_id=forward_caller_id,
            ivr_enabled=ivr_enabled,
            ivr_prompt=ivr_prompt,
            ivr_options=ivr_options,
        )
        return self._client._post(f"/v1/phone-numbers/{id}/voice", data=payload)

    def disable_voice_on_number(self, id: str) -> dict[str, Any]:
        """Disable phone calling on a number"""
        return self._client._delete(f"/v1/phone-numbers/{id}/voice")

    async def acreate_voice_call(
        self,
        to: str,
        *,
        from_number: str | None = None,
        forward_to: str | None = None,
        greeting: str | None = None,
        record_override: bool | None = None,
        transcribe_override: bool | None = None,
        transcription_language: str | None = None,
        amd: bool | None = None,
        voicemail_drop_message: str | None = None,
    ) -> dict[str, Any]:
        """Place an outbound phone call (async)"""
        payload = self._build_payload(
            to=to,
            from_number=from_number,
            forward_to=forward_to,
            greeting=greeting,
            record_override=record_override,
            transcribe_override=transcribe_override,
            transcription_language=transcription_language,
            amd=amd,
            voicemail_drop_message=voicemail_drop_message,
        )
        return await self._client._apost("/v1/voice/calls", data=payload)

    async def alist_voice_calls(
        self,
        *,
        status: str | None = None,
        direction: str | None = None,
        number: str | None = None,
        before: datetime | str | None = None,
        limit: int | None = 50,
    ) -> dict[str, Any]:
        """List phone calls (async)"""
        params = self._build_params(
            status=status,
            direction=direction,
            number=number,
            before=before,
            limit=limit,
        )
        return await self._client._aget("/v1/voice/calls", params=params)

    async def aget_voice_call(self, id: str) -> dict[str, Any]:
        """Get a phone call (async)"""
        return await self._client._aget(f"/v1/voice/calls/{id}")

    async def aend_voice_call(self, id: str) -> dict[str, Any]:
        """Hang up a live call (async)"""
        return await self._client._apost(f"/v1/voice/calls/{id}/end")

    async def aget_voice_call_recording(
        self, id: str, *, as_: str | None = None
    ) -> dict[str, Any]:
        """Get a call recording (async)"""
        params = self._build_params(
            as_=as_,
        )
        return await self._client._aget(
            f"/v1/voice/calls/{id}/recording", params=params
        )

    async def atransfer_voice_call(self, id: str, to: str) -> dict[str, Any]:
        """Blind-transfer a live call (async)"""
        payload = self._build_payload(
            to=to,
        )
        return await self._client._apost(f"/v1/voice/calls/{id}/transfer", data=payload)

    async def aget_voice_call_estimate(
        self,
        to: str,
        *,
        minutes: int | None = 1,
        recording: bool | None = None,
        transcription: bool | None = None,
    ) -> dict[str, Any]:
        """Estimate call cost (async)"""
        params = self._build_params(
            to=to,
            minutes=minutes,
            recording=recording,
            transcription=transcription,
        )
        return await self._client._aget("/v1/voice/calls/estimate", params=params)

    async def acreate_voice_web_session(self) -> dict[str, Any]:
        """Mint a browser softphone session (async)"""
        return await self._client._apost("/v1/voice/calls/web")

    async def adial_voice_web_call(
        self,
        to: str,
        credential_id: str,
        *,
        from_number: str | None = None,
        record_override: bool | None = None,
    ) -> dict[str, Any]:
        """Dial from the browser softphone (async)"""
        payload = self._build_payload(
            to=to,
            credential_id=credential_id,
            from_number=from_number,
            record_override=record_override,
        )
        return await self._client._apost("/v1/voice/calls/web/dial", data=payload)

    async def aenable_voice_on_number(
        self,
        id: str,
        *,
        forward_to: str | None = None,
        recording_enabled: bool | None = None,
        transcription_enabled: bool | None = None,
        transcription_language: str | None = None,
        voicemail_enabled: bool | None = None,
        voicemail_greeting: str | None = None,
        business_hours_enabled: bool | None = None,
        business_hours_timezone: str | None = None,
        business_hours: list[dict[str, Any]] | None = None,
        blocked_callers: list[str] | None = None,
        forward_caller_id: str | None = None,
        ivr_enabled: bool | None = None,
        ivr_prompt: str | None = None,
        ivr_options: list[dict[str, Any]] | None = None,
    ) -> dict[str, Any]:
        """Enable phone calling on a number (async)"""
        payload = self._build_payload(
            forward_to=forward_to,
            recording_enabled=recording_enabled,
            transcription_enabled=transcription_enabled,
            transcription_language=transcription_language,
            voicemail_enabled=voicemail_enabled,
            voicemail_greeting=voicemail_greeting,
            business_hours_enabled=business_hours_enabled,
            business_hours_timezone=business_hours_timezone,
            business_hours=business_hours,
            blocked_callers=blocked_callers,
            forward_caller_id=forward_caller_id,
            ivr_enabled=ivr_enabled,
            ivr_prompt=ivr_prompt,
            ivr_options=ivr_options,
        )
        return await self._client._apost(f"/v1/phone-numbers/{id}/voice", data=payload)

    async def adisable_voice_on_number(self, id: str) -> dict[str, Any]:
        """Disable phone calling on a number (async)"""
        return await self._client._adelete(f"/v1/phone-numbers/{id}/voice")
