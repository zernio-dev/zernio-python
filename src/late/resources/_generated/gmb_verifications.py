"""
Auto-generated gmb_verifications resource.

DO NOT EDIT THIS FILE MANUALLY.
Run `python scripts/generate_resources.py` to regenerate.
"""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

if TYPE_CHECKING:
    from ..client.base import BaseClient


class GmbVerificationsResource:
    """
    gmb_verifications operations.
    """

    def __init__(self, client: BaseClient) -> None:
        self._client = client

    def _build_params(self, **kwargs: Any) -> dict[str, Any]:
        """Build query parameters, filtering None and empty-string values.

        Empty strings are filtered because MCP tool wrappers pass ``""`` as the
        default for optional string args, and the API rejects empty query
        values (e.g. ``platform=``) with a 400. Filtering here keeps both direct
        SDK callers and MCP tool callers safe.

        Enum members are unwrapped to their value: httpx serializes params
        via str(), which yields "ClassName.MEMBER" for Enum members, so
        e.g. ``status=PostStatus.FAILED`` would otherwise reach the API as
        ``status=PostStatus.FAILED`` instead of ``status=failed``.
        """
        from enum import Enum

        def to_camel(s: str) -> str:
            parts = s.split("_")
            return parts[0] + "".join(p.title() for p in parts[1:])

        result: dict[str, Any] = {}
        for k, v in kwargs.items():
            if isinstance(v, Enum):
                v = v.value
            if v is None or v == "":
                continue
            result[to_camel(k)] = v
        return result

    def _build_payload(self, **kwargs: Any) -> dict[str, Any]:
        """Build request payload, filtering None values. Enum members are
        unwrapped to their value so JSON bodies carry e.g. "failed" rather
        than a raw Enum member (plain Enum members are not JSON-serializable).
        """
        from datetime import datetime
        from enum import Enum

        def to_camel(s: str) -> str:
            parts = s.split("_")
            return parts[0] + "".join(p.title() for p in parts[1:])

        result: dict[str, Any] = {}
        for k, v in kwargs.items():
            if isinstance(v, Enum):
                v = v.value
            if v is None:
                continue
            if isinstance(v, datetime):
                result[to_camel(k)] = v.isoformat()
            else:
                result[to_camel(k)] = v
        return result

    def get_google_business_verifications(
        self, account_id: str, *, location_id: str | None = None
    ) -> dict[str, Any]:
        """Get verification state"""
        params = self._build_params(
            location_id=location_id,
        )
        return self._client._get(
            f"/v1/accounts/{account_id}/gmb-verifications", params=params
        )

    def start_google_business_verification(
        self,
        account_id: str,
        method: str,
        *,
        location_id: str | None = None,
        language_code: str | None = None,
        phone_number: str | None = None,
        email_address: str | None = None,
        mailer_contact: dict[str, Any] | None = None,
        context: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Start a verification"""
        params = self._build_params(
            location_id=location_id,
        )
        payload = self._build_payload(
            method=method,
            language_code=language_code,
            phone_number=phone_number,
            email_address=email_address,
            mailer_contact=mailer_contact,
            context=context,
        )
        return self._client._post(
            f"/v1/accounts/{account_id}/gmb-verifications", data=payload, params=params
        )

    def fetch_google_business_verification_options(
        self,
        account_id: str,
        language_code: str,
        *,
        location_id: str | None = None,
        context: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Fetch verification options"""
        params = self._build_params(
            location_id=location_id,
        )
        payload = self._build_payload(
            language_code=language_code,
            context=context,
        )
        return self._client._post(
            f"/v1/accounts/{account_id}/gmb-verifications/options",
            data=payload,
            params=params,
        )

    def complete_google_business_verification(
        self,
        account_id: str,
        verification_id: str,
        pin: str,
        *,
        location_id: str | None = None,
    ) -> dict[str, Any]:
        """Complete a verification"""
        params = self._build_params(
            location_id=location_id,
        )
        payload = self._build_payload(
            pin=pin,
        )
        return self._client._post(
            f"/v1/accounts/{account_id}/gmb-verifications/{verification_id}/complete",
            data=payload,
            params=params,
        )

    async def aget_google_business_verifications(
        self, account_id: str, *, location_id: str | None = None
    ) -> dict[str, Any]:
        """Get verification state (async)"""
        params = self._build_params(
            location_id=location_id,
        )
        return await self._client._aget(
            f"/v1/accounts/{account_id}/gmb-verifications", params=params
        )

    async def astart_google_business_verification(
        self,
        account_id: str,
        method: str,
        *,
        location_id: str | None = None,
        language_code: str | None = None,
        phone_number: str | None = None,
        email_address: str | None = None,
        mailer_contact: dict[str, Any] | None = None,
        context: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Start a verification (async)"""
        params = self._build_params(
            location_id=location_id,
        )
        payload = self._build_payload(
            method=method,
            language_code=language_code,
            phone_number=phone_number,
            email_address=email_address,
            mailer_contact=mailer_contact,
            context=context,
        )
        return await self._client._apost(
            f"/v1/accounts/{account_id}/gmb-verifications", data=payload, params=params
        )

    async def afetch_google_business_verification_options(
        self,
        account_id: str,
        language_code: str,
        *,
        location_id: str | None = None,
        context: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Fetch verification options (async)"""
        params = self._build_params(
            location_id=location_id,
        )
        payload = self._build_payload(
            language_code=language_code,
            context=context,
        )
        return await self._client._apost(
            f"/v1/accounts/{account_id}/gmb-verifications/options",
            data=payload,
            params=params,
        )

    async def acomplete_google_business_verification(
        self,
        account_id: str,
        verification_id: str,
        pin: str,
        *,
        location_id: str | None = None,
    ) -> dict[str, Any]:
        """Complete a verification (async)"""
        params = self._build_params(
            location_id=location_id,
        )
        payload = self._build_payload(
            pin=pin,
        )
        return await self._client._apost(
            f"/v1/accounts/{account_id}/gmb-verifications/{verification_id}/complete",
            data=payload,
            params=params,
        )
