# Zernio API Configuration (for STDIO mode with Claude Desktop)
# For HTTP/SSE mode, users provide their API key via request headers
ZERNIO_API_KEY=your_api_key_here
# LATE_API_KEY is also supported for backwards compatibility

# Server Configuration (optional, has defaults)
HOST=0.0.0.0
PORT=8080

# AI Provider (optional - for content generation)
OPENAI_API_KEY=sk-your_openai_key_here
# ANTHROPIC_API_KEY=sk-ant-your_anthropic_key_here
